<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Github</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Navbar</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item active">
            <a class="nav-link" href="/">Home</a>
          </li>
        </ul>
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <span class="nav-link">Username: <%= userData.login %></span>
          </li>
          <li class="nav-item">
            <span class="nav-link">Name: <%= userData.name %></span>
          </li>
          <li class="nav-item">
            <span class="nav-link">Bio: <%= userData.bio %></span>
          </li>
          <li class="nav-item">
            <span class="nav-link">Followers: <%= userData.followers %></span>
          </li>
          <li class="nav-item">
            <a href="/ghsignout" class="btn btn-primary">Sign Out</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  
  <!-- Modal -->
  <div class="modal" id="myModal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">No Data Available</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <p>No user data available.</p>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
        </div>
      </div>
    </div>
  </div>


  <div class="container">
    <h2>
      <%= userData.login %>
    </h2>
    <h2>
      <%= userData.name %>
    </h2>
    <h2>
      <%= userData.bio %>
    </h2>
    <h2>FOLLOWERS: <%= userData.followers %>
    </h2>
    <br>
    <a href="/ghsignout" class="btn btn-primary">Sign Out</a>
    <div id="userData">
      <h2>User Data:</h2>
      <ul class="list-group">
        <% rows?.forEach(row=> { %>
          <li class="list-group-item">ID: <%= row.id %>, Name: <%= row.name %>, Joined: <%= new
                  Date(row.joined).toISOString() %>, Last Visit: <%= new Date(row.lastvisit).toISOString() %>, Counter:
                    <%= row.counter %>
          </li>
          <% }); %>
      </ul>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"></script>
    <% if (rows == null) { %>
      <script>
          const myModal = new bootstrap.Modal(document.getElementById('myModal'));
          myModal.show();
      </script>
    <% } %>
</body>

</html>